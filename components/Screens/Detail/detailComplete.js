import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const detailComplete = ({navigation}) => {
    return (
        <View>
            <Text>Detail complete</Text>
        </View>
    )
}

export default detailComplete;

const styles = StyleSheet.create({});
